package org.example;

import java.awt.*;
import java.io.File;
import java.io.FileWriter;

public class Main {
    public static void main(String[] args) {

        TextEditor textEditor =new TextEditor();


        //Frame f = new Frame();

        /*

        FileDialog fd = new FileDialog(f, "", FileDialog.SAVE);
        fd.setVisible(true);
        System.out.println(fd.getDirectory()) + "\";

        String fileName = "salam.txt";
        File f = new File(get+ "\\" + fileName);

        /*

        Frame f = new Frame();
        FileDialog fd = new FileDialog(f, "", FileDialog.SAVE);
        fd.setVisible(true);
        System.out.println(fd.getFile());
        String str = "sALm";
        File savedFile = new File(fd.getFile());
        try
        {
            FileWriter fileWriter = new FileWriter(savedFile);
            fileWriter.write(str);
            fileWriter.close();
        }
        catch(Exception exception){

        }
        */

    }
}